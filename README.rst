My Flask is full of Django
==========================

Each folder represents an example project showcasing various features
of Django or Flask. Due to Flask's extension behavior all folders
have been pre-fixed with ``ex_``.

